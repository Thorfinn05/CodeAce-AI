# Dependencies
/node_modules
/.pnp
.pnp.js
.pnp.loader.mjs

# Testing
/coverage

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem
*.lighthouseci/

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
firebase-debug.log*
ui-debug.log
*-debug.log

# Local Environment Variables
.env*.local
.env.development.local
.env.test.local
.env.production.local
# Important: .env should generally be committed if it contains non-sensitive defaults
# or is templated, but if it contains secrets, it should be in .env.local and gitignored.
# The current project setup implies .env may contain secrets, so it's good to have it here
# as a reminder, or if the team decides to move all secrets to .env.local only.
# For this project, .env is committed with API keys, which is not ideal for production.
# Consider moving secrets to .env.local for local development.

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Cypress
/cypress/videos/
/cypress/screenshots/

# Playwright
/test-results/
/playwright-report/

# Turborepo
.turbo

# Firebase
.firebase/ # Firebase CLI cache and emulators data
*firebase-adminsdk*.json # Service account keys
hosting.*.log # Firebase hosting logs
